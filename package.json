{
  "name": "wki-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -p \"[{name}]\" -n SVR,STK,WEB -c green,magenta,cyan \"npm run dev:server\" \"npm run dev:stok\"",
    "dev:server": "cd server && npm run dev",
    "dev:stok": "cd stok && npm run dev",
    "build": "npm run build:server && npm run build:stok",
    "build:server": "cd server && npm run build",
    "build:stok": "cd stok && npm run build",
    "vercel-build": "npm run build",
    "install:all": "npm install && cd server && npm install && cd ../stok && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^10.0.0",
    "wait-on": "7.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "framer-motion": "^12.23.21"
  }
}
